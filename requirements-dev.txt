# Development + test dependencies — mirrors [project.optional-dependencies.dev] in pyproject.toml
# Install: pip install -r requirements-dev.txt
# Or: pip install ".[dev]"
-r requirements.txt
httpx>=0.27.0
pytest>=8.0.0
pytest-xdist>=3.5.0
